/// @desc joinBattle(playerX, playerY, playerDir, heartX, heartY, encounterBubble, heartColor)
/// @function joinBattle
/// @param playerX
/// @param playerY
/// @param playerDir
/// @param heartX
/// @param heartY
/// @param encounterBubble
/// @param heartColor


with oVariables {
	
	joinToBattle = true
	playerX = argument[0]
	playerY = argument[1]
	playerDir = argument[2]
	heartX = argument[3]
	heartY = argument[4]
	encounterBubble = argument[5]
	heartColor = argument[6]
}